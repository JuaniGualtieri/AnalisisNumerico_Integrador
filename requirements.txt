# Dependencias del Trabajo Final Integrador - Análisis Numérico
# Instalar con:   pip install -r requirements.txt
customtkinter>=5.2.0
numpy>=1.24
matplotlib>=3.7
sympy>=1.12
# Pillow lo instala customtkinter automáticamente (manejo de imágenes).
# tkinter viene incluido con Python en Windows (no se instala con pip).
